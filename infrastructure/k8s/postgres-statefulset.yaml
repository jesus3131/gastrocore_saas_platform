apiVersion: v1
kind: Service
metadata:
  name: restopro-postgres
  namespace: restopro
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: restopro-enterprise
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  clusterIP: None
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: restopro
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: restopro-enterprise
spec:
  serviceName: restopro-postgres
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: restopro
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: restopro-secrets
                  key: DB_PASSWORD
            - name: POSTGRES_DB
              value: restopro
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "restopro"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "restopro"]
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
        labels:
          app.kubernetes.io/name: postgres
          app.kubernetes.io/part-of: restopro-enterprise
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: standard
        resources:
          requests:
            storage: 10Gi
